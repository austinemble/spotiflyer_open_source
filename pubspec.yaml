name: spotiflyer_open_source
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^1.1.0
  on_audio_query: ^2.6.0
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  just_audio: ^0.9.28
  rxdart: ^0.27.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
